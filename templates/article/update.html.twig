{% extends 'base.html.twig' %}

{% block title %}Update article{% endblock %}

{% block body %}

    <h1>update an article</h1>

    {{ form(form) }}

    <button>
        <a href="{{ path('getArticles') }}">Cancel</a>
    </button>

{% endblock %}
